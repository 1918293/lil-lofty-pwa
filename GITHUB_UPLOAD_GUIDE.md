# GitHub 上傳

1. 在 `1918293/lil-lofty-pwa` 建立 `chore/organize-v62-pwa` 分支。
2. 上傳本套件中的 `releases/`、`docs/`、`checks/`、`.github/`。
3. 以 `README_V62_PROPOSED.md` 更新根目錄 README。
4. Commit：`chore: organize v62 PWA release`
5. 建立 Draft Pull Request。
6. 不要在實機測試前替換根目錄 `index.html`。

目前 GitHub 連接器未安裝寫入授權，因此對話中建立分支與提交均被 GitHub 回傳 403。
